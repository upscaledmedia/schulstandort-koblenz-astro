---
import data from '../data/worum-es-geht.json';
const w = data.worum_es_geht;

interface Stimme {
  quote: string;
  autor: string;
  url: string;
}
interface Kachel {
  icon?: string;
  bild: string;
  bild_alt: string;
  titel: string;
  headline: string;
  text: string;
  stimme?: Stimme;
}
const kacheln = w.kacheln as Kachel[];
---

<section class="bg-[var(--color-bg)] py-16 md:py-24">
  <div class="container-page">
    <div class="max-w-3xl mb-12 md:mb-16">
      <h2 class="mb-4">{w.ueberschrift}</h2>
      <p class="text-lg text-[var(--color-text-muted)]">{w.untertitel}</p>
    </div>
    <div class="grid md:grid-cols-2 gap-6 md:gap-8">
      {kacheln.map((k) => (
        <article class="bg-[var(--color-bg-card)] p-7 md:p-8 rounded-md border border-[var(--color-border)] flex flex-col h-full">
          <img
            src={k.bild}
            alt={k.bild_alt}
            class="w-full aspect-[16/9] object-cover rounded-md mb-5"
            loading="lazy"
            decoding="async"
          />
          <p class="text-sm uppercase tracking-wider text-[var(--color-text-muted)] font-medium mb-2">
            {k.titel}
          </p>
          <h3 class="font-serif font-bold text-[var(--color-text-primary)] mb-3 text-xl">
            {k.headline}
          </h3>
          <p class="text-[var(--color-text-body)] leading-relaxed text-[15px]">
            {k.text}
          </p>
          {k.stimme && (
            <figure class="mt-5 pl-4 border-l-2 border-[var(--color-accent)]">
              <blockquote class="italic text-[14px] text-[var(--color-text-body)] leading-relaxed">
                „{k.stimme.quote}"
              </blockquote>
              <figcaption class="mt-2 text-[12px] text-[var(--color-text-muted)]">
                — {k.stimme.autor} ·{' '}
                <a
                  href={k.stimme.url}
                  target="_blank"
                  rel="noopener"
                  class="underline decoration-[var(--color-border)] underline-offset-2 hover:text-[var(--color-accent)]"
                >
                  Quelle: openPetition
                </a>
              </figcaption>
            </figure>
          )}
        </article>
      ))}
    </div>
  </div>
</section>
